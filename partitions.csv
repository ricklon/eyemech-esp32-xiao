# Partition table for the 4MB XIAO parts.
#
# WiFi + HTTP server + mbedtls put the app just over the stock 1MB factory
# partition, so it gets 2MB here. NVS must stay — servo calibration lives in it
# under namespace "eyemech".
#
# Name,     Type, SubType, Offset,   Size, Flags
nvs,        data, nvs,     0x9000,   0x6000,
phy_init,   data, phy,     0xf000,   0x1000,
factory,    app,  factory, 0x10000,  2M,
