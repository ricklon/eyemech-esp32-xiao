#pragma once
/* Copy to secrets.h in this directory (gitignored) and fill in. */
#define EYEMECH_WIFI_SSID     "your-ssid"
#define EYEMECH_WIFI_PASSWORD "your-password"
#define EYEMECH_HOSTNAME      "eyemech"
